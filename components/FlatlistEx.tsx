import { FlatList } from "react-native";

const FlatlistEx = () => {
    return (
        <FlatList 
            data={[]}
            renderItem={null}
            keyExtractor={null}
        />
    )
}

export default FlatlistEx;